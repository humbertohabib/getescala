spring:
  datasource:
    url: jdbc:h2:mem:getescala;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

server:
  port: 0

getescala:
  security:
    jwt:
      secret: test-secret-change-me-change-me-change-me-32bytes
